services:
  web:
    build: .
    image: movie-app
    container_name: movie-app
    ports:
      - "8000.8000"
    env_file:
      - .env.server
    volumes:
      - .:/app
      - /opt/movie-data:/app/data:ro
    command: python manage.py runserver 0.0.0.0:8000
    restart: unless-stopped